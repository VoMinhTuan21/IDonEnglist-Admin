export enum PageLayout {
  Authorized = 'authorized',
  UnAuthorized = 'unauthorized',
  Error = 'error',
  Loading = "loading"
}

export enum Skill {
  Listening = 1,
  Reading = 5,
  Writing = 10,
  Speaking = 15
}

export enum CreateTestStep {
  CreateTest = 1,
  ChooseTestPart = 3,
  CreateSections = 6,
}